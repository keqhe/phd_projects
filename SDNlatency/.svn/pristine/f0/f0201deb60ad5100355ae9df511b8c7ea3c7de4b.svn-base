Two priority experiment:

Initially, there are N HIGH priority rules in the table (with same priority)

Here N = 100


Then after some time (we make sure that all the high priority rules are inserted into the flow table)

We insert LOW priority rule in a burst mode, size B
