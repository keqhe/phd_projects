Two priority experiment:

Initially, there are N LOW priority rules in the table (with same priority)

Here N = 100


Then after some time (we make sure that all the low priority rules are inserted into the flow table)

We insert HIGH priority rule in a burst mode, size B
